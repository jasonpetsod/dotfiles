#!/bin/sh

THIS=$SYNCROOT/etc
DEST=$DESTROOT

for f in `ls -1 $THIS`; do
    if [ "$f" == "RCS-disabled" ]; then
        continue
    fi

    full_f=$THIS/$f
    
    ln -sf $full_f $DEST/.$f
done

# make sure the custom (local to host) config dir exists
custom=$DEST/.custom
if [ ! -e $custom ]; then
    mkdir $custom
fi

# add custom config files that need to exist
custom_files=".vimrc .screenrc"
for f in $custom_files; do
    full_f=$custom/$f

    if [ ! -e $full_f ]; then
        touch $full_f
    fi
done
