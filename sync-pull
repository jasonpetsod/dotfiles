#!/bin/bash

SYNCROOT=~/.sync

# cd to the repository
cd $SYNCROOT

hg pull https://hg.quadco.net/sync > /dev/null
hg up > /dev/null

if [ -x $SYNCROOT/sync-run ]; then
    $SYNCROOT/sync-run
else
    echo "sync-run is not executable"
    exit 1
fi
